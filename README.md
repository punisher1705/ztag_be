# ztag_be
A lightweight API gateway (rate limiting, JWT/OAuth2 validation, request signing) sitting in front of your other services, with an AI model flagging anomalous traffic patterns (unusual request sequences, credential stuffing patterns) using LangChain agents reasoning over log streams.
